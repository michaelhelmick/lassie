sudo: false
language: python
cache: pip
python:
  - 2.7
  - 3.6
install:
  - travis_retry pip install tox-travis
  - [[ $TOXENV == py36 ]] && travis_retry pip install coveralls
script: tox
notifications:
  email: false
after_success:
  - [[ $TOXENV == py36 ]] && coveralls
